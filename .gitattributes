# Normalize line endings
* text=auto

# Ensure shell scripts and Gradle wrappers use LF line endings
*.sh text eol=lf
gradlew text eol=lf
android/gradlew text eol=lf

# Treat jars as binary
*.jar binary
